export enum AppConstants {
	/**
	 * Global
	 */
	MODEL_LOADED,
	/*
	 * Navigation
	 */
	SELECT_DATASOURCE,
	NAVIGATE_DATASOURCES_LIST,
	/**
	 * DatasourceList.
	 */
	ADD_FILE,
	/*
	 * Chart renderer
	 */
	PREVIEW_READY,
	DROP_COLUMN,
	DROP_ROW,
	DROP_MEASURE,
	REMOVE_COLUMN,
	REMOVE_ROW,
	REMOVE_MEASURE
}

export enum ApplicationTabs {
	DATASOURCES_VIEW,
	CHART_VIEW
}